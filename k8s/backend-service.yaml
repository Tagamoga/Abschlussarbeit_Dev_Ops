apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8080        # Port, über den der Service erreichbar ist
      targetPort: 8080  # Port im Container
  type: ClusterIP       # Innerhalb des Clusters erreichbar